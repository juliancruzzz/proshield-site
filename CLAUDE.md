@AGENTS.md

# Rules
- When giving terminal/shell commands, ALWAYS include the full directory path to run them from. User is on Windows PowerShell — use semicolons (`;`) not `&&` for chaining, or give commands on separate lines. Never assume the user is in the right directory.
