/**
 * Unified CTA section — all pages use the same V3 design.
 * Re-exported here so existing imports (`from "@/components/sections/cta-section"`)
 * continue to work without touching 30+ files.
 */
export { CtaV3 as CtaSection } from "./cta-v3"
